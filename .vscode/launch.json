{
    // See https://go.microsoft.com/fwlink/?linkid=830387
    // for the documentation about the tasks.json format
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/zig-out/bin/TestApp",
            "args": [],
            "cwd": "${workspaceFolder}/bin",
            "preLaunchTask": "zig build"
        },
    ]
}